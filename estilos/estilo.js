import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    conteiner: {
        flex:1,
        backgroundColor: '#fff',
        alignItems:'center',
        justifyContent: 'center',
    },
    textCursos: {
      color:'#000',
    fontSize: 15,
    },
    textotitulo: {
        color: '#f0f',
        fontSize: 30,
    }
})